// App-wide config constants that aren't game balance.

// The MyEnerge product hub — home base that links to this game, the energy
// course, and the rest of the product suite. The in-game "home" button and the
// title screen both point here. Opened in a new tab so a live demo never loses
// the running game (the PWA itself stays offline-solid; only this link needs net).
export const HOME_URL = 'https://myenerge-eosin.vercel.app/en/dashboard'
